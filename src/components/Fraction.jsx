import React from "react"

const Fraction = ({ firstNum, secondNum }) => (
    <div>
        <p>{firstNum}</p>
        <hr />
        <p>{secondNum}</p>
    </div>
)

export default Fraction